kubernetes:
  client: kube-config-file
  namespace: default
  connect-timeout: 15000
  read-timeout: 15000
  initial-delay: 10000
  fixed-delay: 300000

management:
  endpoints:
    web:
      exposure:
        include: "info, health, metrics, prometheus"